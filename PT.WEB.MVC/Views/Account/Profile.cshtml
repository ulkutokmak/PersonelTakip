@using PT.Entities.ViewModel
@model ProfilePasswordViewModel
@{
    ViewBag.Title = $"Profil Ekranı - {Model.ProfileViewModel.Username}";
}

<h2>@Model.ProfileViewModel.Name @Model.ProfileViewModel.Surname</h2>
@Html.ValidationSummary()

<div class="row">
    <div class=" col-md-6">
        @using (Html.BeginForm("Profile", "Account", FormMethod.Post))
        {
            @Html.HiddenFor(x => x.ProfileViewModel.Id)
            @Html.Raw(ViewBag.sonuc)
            @Html.AntiForgeryToken()
            <div class="form-group">
                @Html.LabelFor(x => x.ProfileViewModel.Username )
                @Html.TextBoxFor(x => x.ProfileViewModel.Username, new { @class = "form-control" ,placeholder="Kullanıcı Adı",@readonly=true})
            </div>
            <div class="form-group">
                @Html.LabelFor(x => x.ProfileViewModel.Name)
                @Html.TextBoxFor(x => x.ProfileViewModel.Name, new { @class = "form-control", placeholder = "Ad" })
            </div>
            <div class="form-group">
                @Html.LabelFor(x => x.ProfileViewModel.Surname)
                @Html.TextBoxFor(x => x.ProfileViewModel.Surname, new { @class = "form-control", placeholder = "Soyad" })
            </div>
            <div class="form-group">
                @Html.LabelFor(x => x.ProfileViewModel.Email)
                @Html.TextBoxFor(x => x.ProfileViewModel.Email, new { @class = "form-control", placeholder = "E-mail" })
            </div>
           
            <div class="row">
                <div class="col-sm-12">
                    <input type="submit" value="Profili Güncelle" class="btn btn-success btn-block" />
                </div>
            </div>
        }
    </div>
    <div class="col-md-6">
        @using (Html.BeginForm("UpdatePassword", "Account", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            <div class="form-group">
                @Html.LabelFor(x => x.PasswordViewModel.OldPassword)
                @Html.TextBoxFor(x => x.PasswordViewModel.OldPassword, new { @class = "form-control",type="password", placeholder = "Eski Şifre" })
            </div>
            <div class="form-group">
                @Html.LabelFor(x => x.PasswordViewModel.NewPassword)
                @Html.TextBoxFor(x => x.PasswordViewModel.NewPassword, new { @class = "form-control", type = "password", placeholder = "Yeni Şifre" })
            </div>
            <div class="form-group">
                @Html.LabelFor(x => x.PasswordViewModel.NewPasswordConfirm)
                @Html.TextBoxFor(x => x.PasswordViewModel.NewPasswordConfirm, new { @class = "form-control", type = "password", placeholder = "Yeni Şifre Tekrar" })
            </div>
            @Html.HiddenFor(x => x.ProfileViewModel.Id)
            <div class="row">
                <div class="col-sm-12">
                    <input type="submit" value="Şifre Güncelle" class="btn btn-primary btn-block" />
                </div>@*her submit içinde bulunduğu formu gönderir.*@
            </div>
        }
    </div>
</div>